"""Panel profiles: physical geometry and (later) layout contracts.

Maps the three supported `PanelProfile`s to concrete pixel dimensions so cards
and renderers reason about size without hardcoding `(w, h)` tuples.
"""
