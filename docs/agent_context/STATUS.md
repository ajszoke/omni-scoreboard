# Status — Omni Scoreboard

_Living status doc. Keep it short; update whenever project state changes (branch, PR, milestone). This is the first thing an agent should read after `AGENTS.md`._

## Now (2026-06-24)

- **Operating mode: the external-review cadence is live** (head/hand — the flagship/CGPT plans, Claude Code implements; *not* `/code-review`). **Round 1 (2026-06-23) → conditional pass; Round 2 (2026-06-24) → conditional pass: architecture signed off, appliance ship deferred.** Adjudication: `~/omni-review/plans/omni-build-fidelity-REVIEWS.md`; verdicts verbatim at `~/omni-review/rounds/round-{1,2}-verdict.md` (the round-2 verdict file is UTC-stamped 06-25 = 06-24 local — keep TZ in mind: the board clock is Eastern, the user is Mountain). State + how-to: the `omni-review-cadence` auto-memory + the `external-review` skill.
- **Round-2 verdict:** **A0 = complete as orchestration architecture** (six gates verified faithful); **B1 = lifecycle-card breadth, NOT "MLB excellence"** (the live card is below the kernel's profile-density contract); **logo/win-prob color direction blessed** with three integration constraints (one asset-aware `MatchupVisualTreatment`/frame; a hardware-tunable `VisualContrastPolicy`; **delay-safe probability** — never attach fresh win-prob to a delayed score). New sequence below. NFL deferred; **PGA before NCAA**.
- Revived repo **bootstrapped from upstream MLB-LED-Scoreboard v9.1.5**; `main` @ `b151e55` = upstream + **PRs #1–#56 merged** (typed pipeline → A0 spine → B1 lifecycle cards → logo/win-prob system). **`main` is green: 671 tests, `omni/` 100% line+branch behind a `fail_under=100` gate; `mypy`/`black` clean.**
- Remotes: `origin` = `ajszoke/omni-scoreboard` (HTTPS), `upstream` = `MLB-LED-Scoreboard/mlb-led-scoreboard` (branch `master`), `legacy` = `ajszoke/omni-scoreboard-legacy` (pre-revival Omni work: `legacy/master`, `legacy/dev`).
- **Known integration gaps (round-2 Highs, all confirmed in-code, all → B1.5/A1):** the logo/meter system is **renderer-only — not wired into the running app** (`AppLoop` builds `RenderContext` with no `LogoStore`; `win_probability` unpopulated; H4); big-play excludes walk-off **singles/SF/BB/HBP** (`NORMAL` < the `HIGH_LEVERAGE` floor, H3); a malformed play can escape the provider boundary (H2); perfect-game absent + `through_inning` premature (H5); no dwell-enforcing display-state machine (H1); every-tick fetch, no `PollPlan` (H6).
- Dev env: **uv + `.venv`** (Py 3.12); emulator runs headless (`http://localhost:8888/`). `pytest`/`pytest-cov` pinned in `requirements.dev.txt`; `starter_code/` excluded from `mypy`/`black`; `*/__main__.py` omitted from coverage. Coverage: `--cov=omni --cov-branch` (see `test` skill). See `docs/dev_setup.md`.
- Physical **`quad_128x64` reference board** is live on the LAN running the pre-revival code; reachable from WSL (`ssh omni-board`). Details in `CLAUDE.local.md`. Deploying the revived repo to it is future work.

## Next — round-2 sequence: B1.5 → A1 (L0 ✅ done; NFL deferred; PGA before NCAA)

- **L0 — preserve the running legacy Pi — ✅ DONE (2026-06-24).** Read-only capture archived off-device + checksum-verified at `~/omni-review/sources/legacy-board-capture-2026-06-24/` (258 working-tree files byte-identical, full `.git`, master `SHA256SUMS`; redacted secrets manifest — an OpenWeatherMap key in `config.json` stays off-repo). Board = **Pi 3A+ / Raspbian 11 / Python 3.9** (vs omni 3.12 — A1 bring-up gap); launch is the rpi-rgb-led-matrix `quad_128x64` invocation (`--led-rows=32 --led-cols=64 --led-chain=4 --led-pixel-mapper=U-mapper --led-slowdown-gpio=3 --led-pwm-bits=6 --led-limit-refresh=128`) with **no autostart** (hand-run `screen`; dark after a power-cycle → A1 safe launcher) and an **Eastern** system clock (pre-move) → set the new repo's IANA tz to `America/Denver`. Off-git delta understood: an ineffective on-device `pitches.py` `SW` edit (the legacy SWPR bug — *already resolved* in omni by `PitchType.SWEEPER = "ST"`, dogfooded), untracked `renderers/util/*`, the live `config.json`, and the downloaded logo/asset set. Pre-existing `ajszoke/nflapi` dep feeds the legacy `data/nfl/` prototype (NFL deferred). Density blueprint preserved: legacy `coordinates/w128h64…json.example` + 25 BDF fonts → B1.5b·M1. (Full SD-card image optional, deferred.)
- **B1.5a — correctness + lifecycle hardening.** (1) close the provider exception boundary — per-play isolation, every parse failure → typed warning/`ProviderError`, can't escape `run_forever` (**H2**); (2) **contextual baseball event scoring** (`BaseballScoringImpact`: runs/tying/go-ahead/walk-off + leverage/inning/favorite/rarity) so *any* scoring play — incl. walk-off single/SF/BB/HBP — is big-play-eligible, attention/dwell chosen after scoring not by intrinsic type (**H3**); (3) correct no-hitter/perfect-game progress — side-specific *completed defensive innings*, reached-base state, `PitchingFeatKind`/`PitchingFeatProgress`, double-no-hitter safe (**H5**); (4) irregular-status policy for `DELAYED`/`SUSPENDED` + resume fixtures (**M2**); (5) hold the last delay-safe frame across the final delay (**H1-adjacent**). Also: `AttentionPolicy` mode invariants (**M6**), `GameEvent` time/identity validation (**L1**), remove the `__main__.py` `# type: ignore[arg-type]` via a typed adapter/`cast` (**L3**).
- **B1.5b — live-dashboard density + visual integration.** Typed `TeamLinescore` (R/H/E), `BatterGameLine`/`PitcherGameLine`, `PitchSnapshot` (velocity+type); **native per-profile layouts to the kernel density contract** — 64x32 = score/inning/count/outs/bases without a crop; 64x64 adds R/H/E + compact batter/pitcher + % bar; 128x64 = full dashboard + short trend, ≥ legacy-photo parity (**M1**); **wire `LogoStore` through app assembly + `RenderContext`**, resolve one asset-aware `MatchupVisualTreatment`/frame, drop the redundant abbr when a logo resolves (**H4, M4**); **delay-safe `WinProbabilitySample`/`Trend`** joined only to the safe display state — *redirects the old PR3d* (never attach fresh win-prob to a delayed score) (**H4**). Goldens for all combos (logo present/missing, clash, dark colors, double-digit R/H/E, long names, extra innings, break, no-hitter badge). A hardware-tunable `VisualContrastPolicy` (CIE76 25 / WCAG 3:1 as defaults, profile/hw overrides).
- **A1a → A1b → A1c — appliance cutover.** **A1a:** a `DisplaySession` dwell/preemption state machine (honor min/max_display; explicit hold-last-frame / idle / offline; recurring yields on cooldown; **H1**) + a typed `PollPlan` decoupled from display (schedule / per-game / favorite cadences, backoff, per-game LKG age+expiry; **H6**) + schedule window spanning prev+current local dates (**M5**). **A1b:** typed `PanelTopology` (1×1 / 2×1 / 2×2), emulator↔Pi agreement, unattended live-game canary (pregame→live→breaks→final), Pi perf (`SetPixel` vs batched; keep `Canvas`, optimize the backend). **A1c:** guarded engine-selector cutover of one board, legacy retained for rollback, soak (several games + a late game + an off-day + a network interruption), drop the legacy flag only after soak.
- **Before a 2nd league (pre-NFL):** typed canonical competitor favorites + card/dedupe keys from the full `LeagueScopedId` + card kind/event identity (**M7**); reconcile the `ProviderUpdate.events` doc vs the feed path (**L5**).
- **Round 3 = appliance sign-off** — gate: *can the new engine run a full MLB game unattended on each profile, no spoilers, no stale-frame traps, correct high-priority events, full profile density, deterministic fault recovery?* Bundle must add (vs round-2): the actual **test suite + `pyproject`/lockfile/CI** (reproduce gates), the **logo tools + runtime assets + provenance/checksums**, **timestamped replay data** (states/events/decisions/prob/failures), the scenario set, density goldens, a display-decision trace, poll/failure traces, redacted topology configs, Pi canary perf. Full per-finding adjudication: REVIEWS round-2.

## Delivered — A0 spine + B1 lifecycle (round-2: signed off as architecture + lifecycle breadth)

- **A0 — orchestration spine + semantic hardening (current).** The deterministic appliance spine in `omni/app/`, built bottom-up and merged (PRs #19–#32): **(1)** invariant hardening → **(2)** typed time/observation/delay contracts (`Clock`, `Observation[T]`, `DelayPolicy` source-time-anchored) → **(3)** `ProviderSupervisor` (last-known-good + backoff) → **(4)** `ContestStore` (snapshot reconcile) → **(5)** `LiveBaseballPipeline` + `DelayedFeed` (delay-safe live cards) → **(6)** `RendererRegistry` (validated dispatch) → **(7)** `AppLoop.run_once` + `DisplaySink` (deterministic tick) → **(8)** `python -m omni.app` live entry (dogfooded vs. real StatsAPI) → **(9)** IANA-tz schedule window (High #5) → **(10)** `AttentionPolicy`/`AttentionMode` types + card field → **(11)** two-level queue rework (group-then-card selection + bounded `BURST` takeover replacing indefinite sticky; recency-orphan cleanup) → **(12)** typed `RenderContext` seam (renderers draw against a render context, not a bare `profile`; B1 adds `now`/contrast as context fields with no renderer-signature churn) → **(13)** fixture-timeline replay (`omni/replay/`: a timestamped `Timeline` format + a `replay()` harness that drives the *real* `AppLoop` under `FakeClock` into deterministic queue traces — closes Med #1 static-scenario→replay; proven with ≥2 simultaneous games + delay + lifecycle entry/removal, the recurring-alert trace lands with B1's alert cards). **All six A0 acceptance gates now genuinely met** — gate 2 (deterministic time-series replay) is substantiated by `omni/replay`, not just inline FakeClock tests — *and* **all five round-1 High findings closed** (event-lineage envelope · source-time delay + no priority-bypass · bounded attention #3 · group/card fairness #4 · time-safety + IANA day #5). **Standing invariant: no sports event bypasses TV delay by display priority.** Remaining A0 (Medium/polish): typed app/device config — then **B1** continues (MLB lifecycle cards, where the event lineage gets fully wired and real cards use `BURST`/`RECURRING`/`BADGE` attention). (Per-card min/max dwell + adaptive polling are A1.)
- **B1 — MLB lifecycle + P0/P1 in `omni/` (not legacy), interleaves with A0 — STARTED.** **(1)** pregame card shipped (PR #31): `PregameCardPayload` + `CardFactory.pregame` + `PregameRenderer` with native 3-profile layouts (`single_64x32` drops the "first pitch" label as an asserted compromise), goldens, and a time-series countdown test; registered in `default_registry`. `RenderContext.now` landed here as its first consumer — the first-pitch countdown is derived from the render clock, so one card snapshot stays correct across ticks. **(2)** live-lifecycle / inning phase shipped (PR #33, closes Med #5): typed `InningPhase` (TOP/MIDDLE/BOTTOM/END) replaces `BaseballGameState`/payload `half`; the provider stops collapsing `Middle`/`End` into the adjacent half; the live renderer shows `T7`/`B7` active and a `MID7`/`END7` break layout that suppresses the stale at-bat (count/outs/bases), with break goldens on all 3 profiles and a replay trace proving the card persists through the breaks. **`HalfInning` fully nuked** — one enum, one `phase` field everywhere (incl. events), and the value-type "re-exported for back-compat" shims in `events`/`cards` removed (importers repointed to `omni.domain.baseball`); no back-compat kept. **(3)** final card shipped (PR #35): `FinalCardPayload` (derives `winner: HomeAway | None` from the score — typed, never position-based) + `CardFactory.final` (finite postgame window via `expires_at`) + `FinalRenderer` (winner bright / loser dimmed / tie both bright; `FINAL` on the larger panels, `FIN` as the asserted `single_64x32` compromise); goldens on all 3 profiles; closes the **winner/loser derivation** P0/P1. **(4)** big-play card shipped (PR #36): `BigPlayCardPayload` + `CardFactory.big_play(game, event, state)` + `BigPlayRenderer` (centred headline/description/score flash; `single_64x32` drops the description as the asserted compromise; goldens on all 3). The factory populates **`source_event_ids=(event.id,)`** and a bounded **`BURST`** attention — closing the *card-side* of **High #1** (event-derived cards retain lineage → dedupable/auditable) and exercising the `BURST` takeover with a real card. Dogfooded vs. a live game (HOU@TOR): real score + populated lineage + burst. **(4b-i)** provider play-by-play parse shipped: the per-game fetch is now `fetch_live_feed(game, now) -> LiveBaseballFeed` (current state **and** the typed events from *one* fetch — no double-fetch) replacing `fetch_game_state` across the spine; `liveData.plays.allPlays` maps into typed `BaseballGameEvent`s with stable lineage ids (`<gamePk>:ab:<atBatIndex>`), source-time anchoring (`about.endTime`), intrinsic per-type importance, and routine-out skipping (unmapped / looking-vs-swinging-ambiguous plays are not emitted). Closes the *provider* half of **High #1** (the pipeline still reads only `feed.state`; `ProviderUpdate.events` stays the schedule-level seam). Dogfooded vs. live HOU@TOR — **25 real events parsed** (both HRs → `ALERT`), proving the expanded `fields` whitelist keeps play-by-play. **(4b-ii)** pipeline event-path shipped — big plays now fire in the running app, closing **High #1** end to end. A new `DelayedEventStream` (the discrete, source-time-anchored sibling of `DelayedFeed`) holds notable events per game and releases each *once* when it clears the same TV delay; the pipeline dedupes by lineage id, **primes the backlog on first sight** (joining a game mid-stream never flushes earlier plays), filters to a big-play band (≥`HIGH_LEVERAGE` — HR/triple/DP/TP), and builds `big_play` cards into the queue (reported in a new `PipelineResult.big_plays`). The big-play card's score now comes from the *event's* post-play `away_score`/`home_score` (parsed from `result.awayScore/homeScore`), not the live state, so a delayed flash never reveals a later, un-aired score; `CardFactory.big_play` dropped its `state` arg. Dogfooded vs. live HOU@TOR — a real HR (`822799:ab:62`, score 4-4) fired through the real pipeline, was selected with `BURST`, and rendered `HOME RUN / … / HOU 4 TOR 4` with lineage intact; backlog suppression verified. Also scrubbed external-review process tags (`round-N`/`High #N`/`verdict`) from code comments — that provenance lives in these docs, not the code. **(5)** no-hitter / perfect-game card vertical shipped: `CardKind.NO_HITTER` + `NoHitterCardPayload` (`pitching_side: HomeAway` + `through_inning` + `perfect`; the feat belongs to the *defending* team, named from the contest, never a stored string) + `CardFactory.no_hitter` + `NoHitterRenderer` (centred `NO-HITTER`/`PERFECT GAME` headline + team + `THROUGH N`; `single_64x32` drops the inning as the asserted compromise; goldens on all 3). It carries a bounded **`RECURRING`** attention (`cooldown` 60s, `max_repeats=None` — resurfaces while the bid is alive, removed when broken) — the first card to use `RECURRING`, and no `expires_at` (pipeline-managed lifecycle, like the live card). Mirrors how big-play #36 shipped the vertical before its wiring. **(6)** the queue now honors `RECURRING`: `next_card` surfaces a `RECURRING` card on its `cooldown` beat — ahead of normal rotation, capped by `max_repeats`, time-anchored via new `_card_last_shown_at`/`_recurring_count` maps — and holds it back between beats, so the no-hitter card reminds periodically without showing every tick; a `BURST` still preempts it (a scoring play outranks a reminder). Pure queue logic (scheduler 100% line+branch); no live no-hitter to dogfood until detection lands. **(7)** no-hitter detection + surfacing shipped — the no-hitter card now appears for real, closing the vertical end to end. `BaseballGameState` gains `away_hits`/`home_hits` (parsed from the linescore R/H/E; `hits` added to the `fields` whitelist); a pure `no_hitter_side(state, *, min_inning)` reads a hitless batting side as the *other* team's bid (persisting across both halves until a hit breaks it, double-no-hitter reports the away drought); the pipeline surfaces/refreshes a `no_hitter` card from the **delay-safe** state (so the bid is revealed and broken only as aired) and removes it on a hit or when the game leaves (new `PipelineResult.no_hitters`; min inning 6). Dogfooded vs. live SEA@PIT — real hits parsed (away 4 / home 8), detection correctly `None`; a forced 0-hit state surfaced `823365:nohitter`, the queue picked it (`RECURRING` due on first sight), it rendered `NO-HITTER / PIT / THROUGH 7`, and a subsequent hit removed it. **(8)** pregame cards now surface in the running app — the pipeline cards the *upcoming* slate, not just LIVE games: `refresh` partitions on status, and SCHEDULED/PREGAME games (`_UPCOMING_STATUSES`) get a `pregame` card with **no feed fetch and no delay** (there is no score to spoil; the renderer derives the first-pitch countdown from the render clock), reported in a new `PipelineResult.pregames` and dropped when the game leaves the upcoming set (it yields to the live card at first pitch, or vanishes when it leaves the slate). Dogfooded vs the live slate (1 final / 10 live / 4 pregame): the four upcoming games carded (`824340:pregame` …) and, with every live game still inside the 45s delay, the queue selected BOS@COL and rendered `FIRST PITCH / BOS / COL / 23m`. **(9)** final cards now surface in the running app, **delay-safe** — the pipeline cards FINAL games, revealing a `final` card only once the result clears the post-game TV delay (the final score is the ultimate spoiler). The reveal is anchored to **first sight of FINAL + lag** (StatsAPI cannot report FINAL before the last out, so first-sight + lag never predates the broadcast's ending — provably no early leak); the live card is dropped when the game leaves the live set, the game shows nothing through the ≤lag catch-up, then the final is ingested (new `PipelineResult.finals`) and lives out its post-game window. Tracking (`_final_pending`/`_final_keys`) is built once — no re-fetch once revealed — and forgotten if the game leaves the slate or resumes. The replay lifecycle trace is now a full pregame → live → final proof through the real loop. Dogfooded vs a live FINAL game (HOU@TOR): the first poll held it embargoed, a poll 45s later revealed `822799:final` rendering `FINAL / HOU 9 / TOR 7` — matching the real final. **(10)** walk-off drain shipped — a held big play now survives the game ending, **completing the pipeline-integration arc**. The per-game event stream is kept alive through the post-final catch-up and the big-play path runs for FINAL games off the same feed fetch, so a walk-off flashes at its own release time (`source_time + lag`) — at or just before the final card — even though the game is already over (spoiler-safe by construction: the big-play delay anchors to when the HR happened = when the broadcast shows it). `_drop_absent` now separates live-scoped teardown (feed/live/no-hitter, gone when the game leaves the live set) from the stream (drains through final; dropped only when the game leaves the slate, or at the final reveal once drained). Dogfooded vs a real HR (HOU@TOR `822799:ab:96`, 9-6) driven across a synthetic LIVE→FINAL flip: the held HR survived the transition, flashed at play+30s (post-final), the final revealed at play+35s, and the stream drained. **All five lifecycle cards (pregame/live/final/big-play/no-hitter) now surface in the running app on one delay-safe spine, and a walk-off cards after the game flips to FINAL.** **(11)** the last raw stringly-typed concept is gone — a typed `PitchType` value object (a StatsAPI-code-valued enum covering all 24 pitch types; the value doubles as the short display token, `label` gives the long name) replaces `pitch_type: str` on the play payload, and the provider parses each at-bat's **decisive (last) pitch** from `playEvents` (`_decisive_pitch_type`, coerced with `try_coerce_enum` so an unknown code yields None, never a leak; the `fields` whitelist now retains the pitch detail). Dogfooded vs the live slate — 134 typed pitches across 10 games (FF/SI/SL/CH/FC/ST/…, incl. 9 sweepers and a HR hit off one), zero unrecognized codes. **(12)** final W/L/S — data layer: a typed `PitchingDecisions` value object (winner/loser plus an optional save, full names) and the provider parses `liveData.decisions` into `LiveBaseballFeed.decisions` (`_parse_decisions` — None until the game ends, for a tie, or a feed without the block; the `fields` whitelist retains it). Dogfooded vs 5 live finals: real W/L/S parsed (e.g. SEA 3-2 PIT → W Kirby, L Keller, S Muñoz), the save correctly optional, live games None. **(13)** final W/L/S — presentation: `FinalCardPayload.decisions` flows from the feed through `CardFactory.final` and the pipeline's `_surface_final` (built from the *current* state at reveal, so a late-filled decision still lands), and `FinalRenderer` draws a W/L/S line of last names — winner bright, loser dimmed (mirroring the score treatment), save bright. Degrades by profile: full W/L/S on `quad_128x64`, W/L on `stack_64x64` (save dropped — no room), nothing on `single_64x32` (already "FIN"); both drops are declared compromise notes and asserted, with regenerated goldens on all three. Dogfooded vs real finals — PHI@WSH and HOU@TOR rendered W/L last names, SEA@PIT rendered `W Kirby / L Keller / S Muñoz` (non-ASCII intact). **The final card is now complete.** **(14)** team logos — base vertical (PR #48): the 30 MLB cap-insignia tiles (`assets/logos/mlb/<abbr>.png`, 20×20, de-trademarked on a flat per-team background; generator at `tools/download_team_logos.py`) now render on the live/pregame/final cards. A hardware-agnostic `Canvas.draw_image` blit primitive (recording/Pillow/matrix canvases) draws a typed `LogoImage`; a `LogoStore` resolves a `LogoAsset` → tile (cached; PNG I/O kept out of the renderer) and is handed to renderers as ambient `RenderContext.logos` (optional → no call-site churn). A shared `draw_team_mark` blits the tile in place of the colour bar on `quad_128x64`/`stack_64x64` (the tile's baked-in background conveys the team colour), falling back to the bar when no store is present; `single_64x32` keeps the bar (a 20px tile doesn't fit a 16px row) — an asserted compromise. Goldens regenerated on quad+stack for all three cards; dogfooded vs the live slate (all 30 tiles load; TEX@MIA / CHC@NYM / CLE@CWS rendered with logos). **Next: PR2 — the logo clash work (in flight).** PR2a (#49): typed `RGBColor.delta_e` (CIE76, ported from legacy). PR2b (#50): first authoritative alt-colour palette. **PR2c (#51): the pipeline pivot** — the crude 20px-tile recolour was replaced (per the head's redirect) by an **MLBStatic-sourced SVG pipeline** (`tools/fetch_mlb_logos.py` + `render_mlb_logo_variants.py`, deps tool-time only): each team gets a base + a clash **alt** tile from official cap art, picking cap-on-light vs cap-on-dark by background luminance so most teams need **no recolour**; a per-team treatment layer (primary-logo strokes, element recolours, knockout, Wikimedia overrides for HOU/BOS/StL/NYM, position-aware de-trademark) covers the rest, through five rounds of contact-sheet review. `omni/providers/mlb_palette.py` is now `LOGO_BASE_COLOR` + `LOGO_ALT_COLOR` (each tile's true bg; a test asserts corner==palette); `omni/domain/logos.py` adds the `LogoKind`/`LogoSurface`/`LogoSource` vocab. **PR2d (in flight): the clash resolver wired into render** — a typed `LogoVariant` {base, alt} (`omni/domain/logos.py`) + `resolve_logo_variants(away, home)` (`omni/renderers/logo_clash.py`) compares the two clubs' effective tile backgrounds — carried on `LogoAsset.preferred_background`, populated by the MLB registry from `LOGO_BASE_COLOR`/`LOGO_ALT_COLOR` — by CIE76 `delta_e`; on a clash (< `CLASH_DELTA_E` = 25, the same perceptual floor the palette holds between a club's own base/alt) exactly one side flips to its alt: the flip that maximises separation, ties resolving to the home side, and never a flip that wouldn't help. A new `draw_matchup_marks` resolves the pair once and the three lifecycle renderers (live/pregame/final) call it in place of the per-side `draw_team_mark`; `single_64x32` (colour bars, no tile) is untouched. Clash golden (NYY@DET → DET shows its orange alt) + resolver unit tests; the six changed files are 100% line+branch covered. Dogfooded vs tonight's real 16-game slate — **8 flip** (NYY@DET, CHC@NYM, PHI@WSH, HOU@TOR, KC@TB, LAD@MIN, AZ@STL), the threshold discriminating right at the boundary (BOS@COL navy-vs-black Δe 25.2 stays distinct). **Test-hygiene pass shipped** — `omni/` is now at **100% line+branch with a `fail_under = 100` gate** (`[tool.coverage.report]`, `branch = true`) so a drop fails CI instead of drifting. The lingering misses were closed *correctly*: `EnumMixin.to_json_value` had drifted from the kernel/Alpine mixin design (a redundant `StrEnumMixin.to_json_value` override had been added, making the base dead) — the override was removed so str-enums serialize through the base again (the kernel design; only `IntEnumMixin` overrides, since an int's JSON identity is its name); `PillowCanvas.width/height` got a dimension assertion. Warnings are now **errors** (`[tool.pytest.ini_options]` `filterwarnings`) with a single targeted ignore for RGBMatrixEmulator's import-time asyncio-policy deprecation; the upstream-only mypy `annotation-unchecked` note is silenced (`omni/` is fully typed). Upstream-vendored tests (`test_custom_config.py` etc.) left untouched. **Win-probability meter — PR3a (in flight): the value-lift primitive.** `RGBColor.value_lifted(on=black, min_contrast=3.0)` brightens a dim brand colour to the WCAG-3:1 legibility floor — raising HSV *value* (hue+saturation kept, so it stays the team's colour) then shedding saturation toward white only if full brightness still falls short — so the meter's colours read on the black panel (the Angels' navy, 1.64:1, lifts to legible). A palette sweep asserts every base+alt meter colour lifts clear. **The rest of the arc is grounded and free-data:** StatsAPI `game_contextMetrics` serves `home`/`awayWinProbability` (0–100, confirmed live, e.g. CHC@NYM 26/74); the legacy meter (`legacy:renderers/games/teams.py`) painted the *raw* team accent with no lift — that's the dim-accent bug this fixes. **PR3b (in flight): typed win-prob data.** A `WinProbability(home, away)` value object (`omni/domain/baseball.py`; `favored: HomeAway | None`, `percent_for(side)`, percentages validated 0–100) + `MlbStatsApiProvider.fetch_win_probability(game)` parsing StatsAPI `game_contextMetrics` — a *separate*, caller-cadenced call (distinct from the feed fetch, injected as `fetch_context_metrics`); returns None when no probability is in the payload (pregame/absent), raises `ProviderError` only on a fetch failure. Dogfooded vs the live slate (typed probabilities parsed; a live game read 26/74). **PR3c (in flight): the win-prob meter renderer.** `omni/renderers/win_meter.py::draw_win_meter` draws a per-team vertical gauge beside each logo (quad+stack; `single_64x32` omits it, an asserted compromise), filled bottom-up to each side's win%, in the team's **freed** colour — the value-lifted palette colour of the logo variant it is *not* showing (so the gauge always differs from the tile and a dim navy still reads). Gated on a new optional `LiveBaseballCardPayload.win_probability` (None → no meter; existing goldens unchanged). Quad+stack goldens; dogfooded (CHC@NYM 100/0 → one full gauge in CHC's freed red). Perfect-game still deferred (baserunner data).
- **Open for round-2 review — live-card density gap.** The quad live card currently renders ~the *Minimal* column (score/inning/count/outs/bases) where the roadmap (`ROADMAP.md` display-roles table; kernel `ROADMAP.md`) calls quad the **full live dashboard**. Versus the legacy board (`downloads/IMG_4658.jpeg`) the new card is missing **R/H/E** (hits already modelled as `away_hits`/`home_hits`, errors trivial to add), the **pitcher line** + **batter line** (free from the same feed — legacy `data/game.py` `batter_stats`/`pitcher_stats`), **pitch velocity** (`startSpeed`; `PitchType` already modelled), and it adds a **redundant abbr** next to the logo (legacy uses that space for H/E). The win-prob meter above is the *narrow* placement (a 2px sliver); the dense pass would give it legacy-style prominence. Sequencing the density pass to parity → beyond-parity is a round-2 topic. **Also for the bundle:** ✅ the running board's on-device state — incl. untracked assets/scripts + the live `config.json` — is now archived off-device and checksum-verified (L0, 2026-06-24); git history also lives on the `legacy` remote.
- **A1 — appliance cutover + soak (after B1).** Dwell/preemption state machine; adaptive polling decoupled from display (no spoiler leak); snapshot reconciliation; typed physical topology for 1×1 / 2×1 / 2×2; emulator-per-profile + physical canary with telemetry; cut `main.py` over behind a temporary legacy flag only after full-game replay + live canary are clean.
- **Deferred (head's call):** C/NFL (until the MLB loop runs multiple simultaneous games + card kinds without starvation), device UX (portal/updater/buttons), MLB P2/P3 visuals, in-game win probability, then NBA/NHL/PGA/NCAA.
- **Round 2 (sign-off)** runs after A0/B1 with the head's requested bundle (runtime/config surface, legacy reference, real goldens, timestamped fixtures, queue traces, device configs, Pi perf). See REVIEWS.

## Done

- Renamed old fork → `omni-scoreboard-legacy` (history preserved).
- Created revived `omni-scoreboard` (Path B: normal repo; `upstream` remote wired for syncing).
- Cloned to `/opt/omni-scoreboard`, set remotes, pushed `main`.
- **PR #1 merged** — agent context pack + project-local Claude Code.
- **PR #2 merged** — typed-domain core (`omni/core/`); pinned `pytest`, excluded `starter_code/` from lint/type.
- **PR #3 merged** — typed domain objects (`omni/domain/`: `Competitor`/`Team`/`Contest`).
- **PR #4 merged** — typed event layer (`omni/events/`: `GameEvent` + per-sport event-type enums).
- **PR #5 merged** — first card + renderer vertical (`omni/cards`, `omni/panels`, `omni/renderers`; `quad_128x64` + golden).
- **PR #6 merged** — QC hardening (`HalfInning` enum, validation, +18 gap tests, branch coverage).
- **PR #7 merged** — multi-profile renderer (`LiveBaseballRenderer` natively renders all three profiles; `single_64x32` is an explicit, tested compromise; `stack_64x64`/`single_64x32` goldens; `assert_never` dispatch).
- **PR #8 merged** — MLB StatsAPI provider boundary (`omni/providers/`: `Provider`/`ProviderUpdate`, `MlbTeamRegistry`, `schedule()` → typed `TeamGame` contests; fixture-driven, dogfooded live).
- **PR #9 merged** — live game state + `CardFactory` (`omni/domain/baseball.py` `BaseballGameState`; provider `fetch_game_state`; `omni/cards/factory.py`); closes provider → card → renderer, end-to-end test + dogfood.
- **PR #10 merged** — on-screen emulator preview + `MatrixCanvas` (`Canvas` → LED `SetPixel`, pixel-identical to goldens); `python -m omni.preview --profile … --fixture …`; Phase-2 `omni preview` DoD.
- **PR #11 merged** — `PriorityScorer` (`omni/queue/priority.py`): explainable `CardPriority` (band + score + reasons) from live state; first Phase-4 queue piece.
- **PR #12 merged** — `DelayBuffer[T]` (`omni/queue/delay_buffer.py`): generic TV-delay holding buffer (`push`/`release`); no score spoilers.
- **PR #13 merged** — `InterleavedCardQueue` (`omni/queue/scheduler.py`): fair, priority-weighted rotation across contests (dedupe, sticky alerts, profile-aware); **completes Phase 4**.
