# Status — Omni Scoreboard

_Living status doc. Keep it short; update whenever project state changes (branch, PR, milestone). This is the first thing an agent should read after `AGENTS.md`._

## Now (2026-06-23)

- **Operating mode: the external-review cadence is live** (head/hand — the flagship/CGPT plans, Claude Code implements; *not* `/code-review`). **Round 1 (build-fidelity) is reviewed → conditional pass.** Full adjudication + open-question dispositions: `~/omni-review/plans/omni-build-fidelity-REVIEWS.md`; verdict verbatim at `~/omni-review/rounds/round-1-verdict.md`. State + how-to: the `omni-review-cadence` auto-memory + the `external-review` skill.
- **Verdict outcome:** both deliberate deviations **blessed** (per-card renderers + `Canvas`; queue-before-polish spike). The typed foundation is faithful, **but Phase 4 / M4 is reclassified as a *snapshot-card queue vertical slice*, not shipped** — six semantic gaps (event→card lineage, source/observation identity, no priority-based delay bypass, bounded sticky, intra-contest fairness, deterministic time-series replay) are now A0 acceptance gates. Next phase sequenced **A0 → B1 → A1** (C/NFL deferred).
- Revived repo **bootstrapped from upstream MLB-LED-Scoreboard v9.1.5**; `main` = upstream + PRs #1–#16 (typed pipeline through Phase 4; then handoff docs, the **frozen kernel** at `docs/agent_context/kernel/`, the `external-review` skill, and the ARCHITECTURE as-built reconciliation). PR #17 (review-home relocation) open.
- Remotes: `origin` = `ajszoke/omni-scoreboard` (HTTPS), `upstream` = `MLB-LED-Scoreboard/mlb-led-scoreboard` (branch `master`), `legacy` = `ajszoke/omni-scoreboard-legacy` (pre-revival Omni work: `legacy/master`, `legacy/dev`).
- **PRs #1–#16 merged and cleaned up;** #17 open. `main` is green.
- **Typed pipeline (provider → score → delay → interleave → render) is whole as a vertical slice:** each stage typed, tested, dogfooded. 269 tests; `omni/` ~99% (queue/providers/factory 100% incl. branch); `mypy`/`black` clean. Hardening to appliance-grade is the A0 work below.
- Dev env: **uv + `.venv`** (Py 3.12); emulator runs headless (`http://localhost:8888/`). `pytest`/`pytest-cov` pinned in `requirements.dev.txt`; `starter_code/` excluded from `mypy`/`black`; `*/__main__.py` omitted from coverage. Coverage: `--cov=omni --cov-branch` (see `test` skill). See `docs/dev_setup.md`.
- Physical **`quad_128x64` reference board** is live on the LAN running the pre-revival code; reachable from WSL (`ssh omni-board`). Details in `CLAUDE.local.md`. Deploying the revived repo to it is future work.

## Next — A0 → B1 → A1 (head-sequenced; C/NFL deferred)

- **A0 — orchestration spine + semantic hardening (current).** Stand up `omni/app/`: a `Clock` protocol (`SystemClock`/`FakeClock`), typed app/device config (profile, physical topology, IANA tz, favorites, delay/poll policy), `ProviderSupervisor` (timeout/backoff, last-known-good), `ContestStore` (reconcile/expire), `Pipeline` (observation/event → delay → factory/scorer → queue), `RendererRegistry` + `DisplaySink`, and a deterministic `run_once(now)` (the infinite loop is a thin wrapper). Domain-contract changes: typed `Observation[T]` + populated `source_event_ids`; `DelayPolicy`/`DelayAnchor` anchored to `source_time` (`eligible_at = max(observed_at, source_time + lag)`); `AttentionPolicy`/`AttentionMode` separated from `DisplayPriority` (bounded takeover/cooldown); two-level queue selection (rotation-group then card). **Invariant: no sports event bypasses TV delay by display priority** — a sports ALERT is the most spoiler-heavy content (system/setup status is a separate policy). *Increments landed: **A0(1)** invariant hardening (#19 — fail-fast `DisplayTiming`, `league==id.league`, unconditional competitor derivation, doc refresh); **A0(2)** typed time/observation contracts (`omni/app/clock.py` `Clock`/`SystemClock`/`FakeClock`, `core/observation.py` `Observation[T]` + `ProviderSequence`, `queue/delay_policy.py` `DelayPolicy`/`DelayAnchor` source-time-anchored — additive, not yet wired); **A0(3)** `ProviderSupervisor` (`omni/app/supervisor.py` — failure isolation + last-known-good snapshot with typed `ProviderStatus`/age + exponential backoff); **A0(4)** `ContestStore` (`omni/app/contest_store.py` — reconciles provider snapshots into the live contest set: add/update/remove via a deterministic `Reconciliation`, stale out-of-order snapshots ignored by `observed_at`); **A0(5)** live-baseball pipeline (`omni/app/pipeline.py` `LiveBaseballPipeline` + `queue/delayed_feed.py` `DelayedFeed` — observes live state, holds it in a per-game delayed feed so what surfaces is lag-old and never spoils, scores + builds the card, ingests it, and drops cards/feeds for games that leave the live slate); **A0(6)** `RendererRegistry` (`omni/renderers/registry.py` — routes `(Sport, CardKind)` → renderer and validates card/renderer/canvas agree on the profile at dispatch; `default_registry()` replaces preview's hard-coded `LiveBaseballRenderer`); **A0(7)** `DisplaySink` + `AppLoop.run_once(now)` (`omni/app/display.py` + `omni/app/loop.py` — the deterministic spine tick: poll → reconcile → pipeline → pick card → render → commit, with render/display failures isolated; verified end-to-end under `FakeClock` incl. last-known-good survival of a provider outage). **The deterministic spine is whole.** **A0(8)** live entry (`omni/app/__main__.py` + `omni/app/runner.py` — `python -m omni.app --emulated` wires the real `MlbStatsApiProvider` + emulator `MatrixDisplaySink` via `build_loop`/`run_forever`; dogfooded against live StatsAPI: fetched today's real 15-game schedule, reconciled it, no crash). Next increment: **IANA-tz schedule window** (High #5 — the provider still picks the schedule via UTC `now.date()`, so US evenings can request tomorrow). Still pending in A0: `AttentionPolicy`/two-level queue (High #3/#4), `RenderContext` threading, typed app/device config; proper min/max-display dwell + adaptive polling are A1.*
- **B1 — MLB lifecycle + P0/P1 in `omni/` (not legacy), interleaves with A0.** Pregame / live-lifecycle (TOP/MIDDLE/BOTTOM/END) / final / big-play (typed `BaseballGameEvent` + lineage) / no-hitter-perfect-game (badge + bounded recurring) cards. P0/P1: pitch-type value object (sweeper mapped), typed logo/contrast policy, anchored text primitives, winner/loser derivation. Every new card: native 3-profile layouts, explicit compromise notes, goldens, and a time-series fixture.
- **A1 — appliance cutover + soak (after B1).** Dwell/preemption state machine; adaptive polling decoupled from display (no spoiler leak); snapshot reconciliation; typed physical topology for 1×1 / 2×1 / 2×2; emulator-per-profile + physical canary with telemetry; cut `main.py` over behind a temporary legacy flag only after full-game replay + live canary are clean.
- **Deferred (head's call):** C/NFL (until the MLB loop runs multiple simultaneous games + card kinds without starvation), device UX (portal/updater/buttons), MLB P2/P3 visuals, in-game win probability, then NBA/NHL/PGA/NCAA.
- **Round 2 (sign-off)** runs after A0/B1 with the head's requested bundle (runtime/config surface, legacy reference, real goldens, timestamped fixtures, queue traces, device configs, Pi perf). See REVIEWS.

## Done

- Renamed old fork → `omni-scoreboard-legacy` (history preserved).
- Created revived `omni-scoreboard` (Path B: normal repo; `upstream` remote wired for syncing).
- Cloned to `/opt/omni-scoreboard`, set remotes, pushed `main`.
- **PR #1 merged** — agent context pack + project-local Claude Code.
- **PR #2 merged** — typed-domain core (`omni/core/`); pinned `pytest`, excluded `starter_code/` from lint/type.
- **PR #3 merged** — typed domain objects (`omni/domain/`: `Competitor`/`Team`/`Contest`).
- **PR #4 merged** — typed event layer (`omni/events/`: `GameEvent` + per-sport event-type enums).
- **PR #5 merged** — first card + renderer vertical (`omni/cards`, `omni/panels`, `omni/renderers`; `quad_128x64` + golden).
- **PR #6 merged** — QC hardening (`HalfInning` enum, validation, +18 gap tests, branch coverage).
- **PR #7 merged** — multi-profile renderer (`LiveBaseballRenderer` natively renders all three profiles; `single_64x32` is an explicit, tested compromise; `stack_64x64`/`single_64x32` goldens; `assert_never` dispatch).
- **PR #8 merged** — MLB StatsAPI provider boundary (`omni/providers/`: `Provider`/`ProviderUpdate`, `MlbTeamRegistry`, `schedule()` → typed `TeamGame` contests; fixture-driven, dogfooded live).
- **PR #9 merged** — live game state + `CardFactory` (`omni/domain/baseball.py` `BaseballGameState`; provider `fetch_game_state`; `omni/cards/factory.py`); closes provider → card → renderer, end-to-end test + dogfood.
- **PR #10 merged** — on-screen emulator preview + `MatrixCanvas` (`Canvas` → LED `SetPixel`, pixel-identical to goldens); `python -m omni.preview --profile … --fixture …`; Phase-2 `omni preview` DoD.
- **PR #11 merged** — `PriorityScorer` (`omni/queue/priority.py`): explainable `CardPriority` (band + score + reasons) from live state; first Phase-4 queue piece.
- **PR #12 merged** — `DelayBuffer[T]` (`omni/queue/delay_buffer.py`): generic TV-delay holding buffer (`push`/`release`); no score spoilers.
- **PR #13 merged** — `InterleavedCardQueue` (`omni/queue/scheduler.py`): fair, priority-weighted rotation across contests (dedupe, sticky alerts, profile-aware); **completes Phase 4**.
